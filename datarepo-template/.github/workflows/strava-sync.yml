name: Strava sync

on:
  schedule:
    - cron: '17 */4 * * *' # every 4h, off the top of the hour
  workflow_dispatch: {}

concurrency:
  group: strava-sync
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Pull new Strava activities
        env:
          STRAVA_CLIENT_ID: ${{ secrets.STRAVA_CLIENT_ID }}
          STRAVA_CLIENT_SECRET: ${{ secrets.STRAVA_CLIENT_SECRET }}
          STRAVA_REFRESH_TOKEN: ${{ secrets.STRAVA_REFRESH_TOKEN }}
          # Optional: set to the same password used in the app to write
          # encrypted GPS shards (see README "Encryption").
          ENCRYPTION_PASSWORD: ${{ secrets.ENCRYPTION_PASSWORD }}
          # Optional bounds (repo *variables*, not secrets — see README).
          # STRAVA_SYNC_AFTER: YYYY-MM-DD floor; unset means the first run
          #   records today, so setup doesn't pull years of history.
          # STRAVA_SYNC_TYPES: sport_type allowlist, e.g. "Run,TrailRun,Ride".
          # STRAVA_MAX_PAGES:  page cap, 100 activities per page (default 20).
          STRAVA_SYNC_AFTER: ${{ vars.STRAVA_SYNC_AFTER }}
          STRAVA_SYNC_TYPES: ${{ vars.STRAVA_SYNC_TYPES }}
          STRAVA_MAX_PAGES: ${{ vars.STRAVA_MAX_PAGES }}
        run: node scripts/strava-sync.mjs

      - name: Commit and push
        run: |
          git config user.name "strava-sync[bot]"
          git config user.email "strava-sync@users.noreply.github.com"
          git add data/strava
          if git diff --cached --quiet; then
            echo "No new activities"
            exit 0
          fi
          git commit -m "Sync Strava activities"
          git push || (git pull --rebase && git push)
